[project]
name = "kimmizo-setup"
version = "1.0.0"
description = "Project bootstrap for a Thai secretary, custom agent team, capability routing and memory."
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pytest>=8,<10"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
