# Project Capsule Template

These files are copied or merged into a target project by `scripts/kimmizo.py`.
`AGENTS.md`, `.codex/config.toml`, and `.gitignore` always use versioned, checksummed managed blocks. Existing project-owned content remains outside those blocks.
